# ---------------------------------------------------------------------------
# Copy to .env.local and fill in values. Never commit .env.local.
# ---------------------------------------------------------------------------

# Auth.js (required in production)
# Generate: bunx auth secret
AUTH_SECRET=

# Auth.js base URL (required in production so callbacks work)
# Local: omit or use http://localhost:3000
# Vercel: https://your-domain.vercel.app (e.g. https://eoslt.vercel.app)
# AUTH_URL=https://eoslt.vercel.app

# Google OAuth (create at https://console.cloud.google.com/apis/credentials)
# Redirect URIs: add BOTH:
#   http://localhost:3000/api/auth/callback/google
#   https://eoslt.vercel.app/api/auth/callback/google (or your production URL)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Convex (set by `bunx convex dev` for dev; use prod URL in production)
# NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Stream OBS: base URL for overlay browser sources (optional). Overrides server-detected origin (LAN IP when you use localhost).
# Production: https://your-app.vercel.app
# NEXT_PUBLIC_STREAM_OVERLAY_ORIGIN=http://192.168.1.206:3000

# Stream OBS: Vercel sets NEXT_PUBLIC_STREAM_OBS_VERCEL=1 at build time (see next.config.ts).
# When true, OBS WebSocket runs in your browser so Tailscale/private OBS IPs work (the serverless
# host cannot reach them). HTTPS pages require wss:// to OBS or the browser may block ws://
# (mixed content); use local dev, self-host on your tailnet, or terminate TLS in front of OBS.
# Force transport: NEXT_PUBLIC_STREAM_OBS_TRANSPORT=browser | server

# PoolHub SQL Server (read-only). Example:
# Server=host;Database=dbname;User ID=user;Password=pass;MultipleActiveResultSets=true
POOLHUB_DATABASE_URL=
