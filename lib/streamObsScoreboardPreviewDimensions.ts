/**
 * Pixel size for the stream dashboard “scoreboard preview” card (compact scorecard + `p-3` padding).
 * Used when creating/updating the OBS Browser Source for the 1v1 scoreboard so the source matches
 * the preview; streamers can scale the source in OBS if they want it larger on canvas.
 */
export const SCOREBOARD_PREVIEW_CARD_OUTER_WIDTH_PX = 560;
/** Compact scorecard + `p-3`; padded so borders/shadow are not clipped in OBS. */
export const SCOREBOARD_PREVIEW_CARD_OUTER_HEIGHT_PX = 120;
