/**
 * Pixel size of the tournament results card in the stream dashboard preview and the matching
 * OBS browser_source resolution (CreateInput / SetInputSettings when wiring results).
 */
export const RESULTS_PREVIEW_CARD_OUTER_WIDTH_PX = 350;

/**
 * Outer height matching preview card padding + “Results” title + 16 table rows (dashboard variant).
 * Slightly padded so occasional name wrap does not clip inside OBS.
 */
export const RESULTS_PREVIEW_CARD_OUTER_HEIGHT_PX = 820;
