/**
 * Minimal OBS Browser Source footprint for the SFX listener (audio only; no meaningful visuals).
 */
export const SFX_BROWSER_SOURCE_WIDTH_PX = 50;
export const SFX_BROWSER_SOURCE_HEIGHT_PX = 50;

/** Default mixer level when the SFX browser source is first created (OBS dB). */
export const SFX_BROWSER_SOURCE_INITIAL_VOLUME_DB = -15;
